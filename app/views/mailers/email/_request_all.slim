/ public valiable template for a request
table
  thread
    tr
      th 姓名
      th 性别
      th 微信
      th 地点
  tbody
    tr
      td= @req.name
      td= @req.gender
      td= @req.weixin
      td= @req.place
